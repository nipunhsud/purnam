<div class="mx-auto md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">Editing project stakeholder</h1>

  <%= turbo_frame_tag @project_stakeholder do %>
    <%= render "form", project_stakeholder: @project_stakeholder %>
    <%= link_to "Back to project", project_path(@project), class: "ml-2 rounded-lg py-3 px-5 bg-gray-100 inline-block font-medium" %>
  <% end %>
</div>
